# Runtime dependencies for the OpenCredit AI backend (Person 3).
# The agent (Person 1) and ML (Person 2) packages are part of this repo.

fastapi==0.141.1
uvicorn==0.52.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
openai>=1.0.0

# Development / testing
pytest==9.1.1
httpx>=0.27.0
